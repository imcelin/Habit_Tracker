# Habit Tracker Frontend

Simple React frontend for the Habit Tracker homework project.

## Start

```bash
npm install
npm run dev
```

The frontend expects the backend to run on `http://localhost:3000`.
